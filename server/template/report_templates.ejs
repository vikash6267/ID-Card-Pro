<!DOCTYPE html>
<html>
<head>
  <title>Student Report</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 40px;
      padding: 20px;
      background-color: #f8f9fa;
      position: relative;
    }

    /* Visit Website Banner */
    .visit-website {
      text-align: center;
      font-size: 16px;
      color: #007bff;
      font-weight: bold;
      position: absolute;
      top: 10px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 1;
      cursor: pointer;
    }

    .visit-website:hover {
      color: #0056b3;
      text-decoration: underline;
    }

    /* Watermark */
    body::before, body::after {
      content: "www.cardpro.co.in";
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%) rotate(45deg);
      font-size: 50px;
      color: rgba(0, 0, 0, 0.1);
      font-weight: bold;
      z-index: -1;
      white-space: nowrap;
    }

    /* Additional rotated watermarks */
    body::after {
      content: "www.cardpro.co.in";
      top: 30%;
      left: 30%;
      transform: translate(-30%, -30%) rotate(45deg);
      font-size: 50px;
      color: rgba(0, 0, 0, 0.1);
      font-weight: bold;
      z-index: -1;
    }

    /* Table Styling */
    .header {
      text-align: center;
      margin-bottom: 20px;
    }

    .logo {
      width: 200px;
    }

    .title {
      font-size: 28px;
      font-weight: bold;
      color: #333;
      margin-top: 10px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: #fff;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    th, td {
      padding: 12px;
      border: 1px solid #ddd;
      text-align: center;
    }

    th {
      background: #007bff;
      color: #fff;
      font-weight: bold;
    }

    tr:nth-child(even) {
      background: #f2f2f2;
    }

    tr:hover {
      background: #ddd;
    }

    /* Total Row Styling */
    .total-row {
      background: #28a745 !important;
      color: #fff;
      font-weight: bold;
    }
  </style>
</head>
<body>

  <!-- Visit Website Banner -->
  <div class="visit-website">
    Visit Website - <a href="https://www.cardpro.co.in" target="_blank" style="color: inherit;">www.cardpro.co.in</a>
  </div>

  <!-- Company Logo -->
  <div class="header">
    <img src="https://cardpro.co.in/login.jpg" alt="Company Logo" class="logo">
    <div class="title">Student Report</div>
  </div>

  <!-- School Name -->
  <h2>Vendor : <%= schoolName %></h2>

  <!-- Report Table -->
  <table>
    <thead>
      <tr>
        <th>Class</th>
        <th>Panding</th>
        <th>Ready to Print</th>
        <th>Printed</th>
      </tr>
    </thead>
    <tbody>
      <% 
        let totalPanding = 0;
        let totalReadyToPrint = 0;
        let totalPrinted = 0;
        
        result.forEach(classData => { 
          totalPanding += classData.Panding;
          totalReadyToPrint += classData["Ready to print"];
          totalPrinted += classData.Printed;
      %>
        <tr>
          <td><%= classData.class %></td>
          <td><%= classData.Panding %></td>
          <td><%= classData["Ready to print"] %></td>
          <td><%= classData.Printed %></td>
        </tr>
      <% }); %>

      <!-- Total Row -->
      <tr class="total-row">
        <td>Total</td>
        <td><%= totalPanding %></td>
        <td><%= totalReadyToPrint %></td>
        <td><%= totalPrinted %></td>
      </tr>
    </tbody>
  </table>

</body>
</html>
